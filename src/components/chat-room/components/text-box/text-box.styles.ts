import { css } from '@emotion/core';

export const textBox = css`
  margin-left: auto;
  margin-right: auto;
`;

export const textarea = css`
  padding: 1rem;
  resize: none;
  white-space: pre-wrap;
`;
